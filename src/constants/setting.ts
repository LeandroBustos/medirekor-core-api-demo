export enum SettingGroupTypes {
    USER = 'USER',
    ORGANIZATION = 'ORGANIZATION'
}

export enum SettingStates {
    ACTIVE = 'ACTIVE',
    DELETED = 'DELETED',
    DISABLED = 'DISABLED'
}